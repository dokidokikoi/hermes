# postgresql:
#   host: 127.0.0.1
#   port: 5432
#   username: izumi
#   database: izumi
#   password: 1234567
sqlite:
  database: /data/db/izumi.db
app: 
  port: 19876
log:
  - filename: ./log/info.log
    level_range: "[info,error)"
  - filename: ./log/error.log
    level_range: "[error,)"

plugin:
  lua:
    enabled: true
    script_dir: ./scraper/plugins
    vm_pool_size: 10
    hot_reload: true
    execution_timeout: 30s
  
